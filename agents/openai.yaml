interface:
  display_name: "Mary Workflow"
  short_description: "Run milestone workflows and close-read research papers"
  default_prompt: "Use $mary-workflow to close-read an arXiv paper with parse-quality gating, or manage project milestones."

policy:
  allow_implicit_invocation: true
