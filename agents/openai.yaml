interface:
  display_name: "Mary Workflow"
  short_description: "Run milestones and write grounded paper articles"
  default_prompt: "Use $mary-workflow to close-read an arXiv paper and write a readable article with machine-validated source evidence."

policy:
  allow_implicit_invocation: true
