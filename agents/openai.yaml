interface:
  display_name: "Mary Workflow"
  short_description: "Build full project understanding and run milestone workflows"
  default_prompt: "Use $mary-workflow to fully read and understand the repository, plan milestones, run, inspect status, stop, debug, or archive cycles with /mw-cycle."

policy:
  allow_implicit_invocation: true
