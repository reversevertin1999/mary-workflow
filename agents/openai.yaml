interface:
  display_name: "Mary Workflow"
  short_description: "Run simple project prompt workflows"
  default_prompt: "Use $mary-workflow to initialize and run a small local prompt workflow."

policy:
  allow_implicit_invocation: true
